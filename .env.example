# Copiare in .env.local. Nessun segreto in questo file.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Chiave pubblica (anon o publishable): mai la secret/service_role.

# URL pubblico dell app (link nelle email di auth). Su Vercel se assente si usa VERCEL_URL.
SITE_URL=http://localhost:3000
